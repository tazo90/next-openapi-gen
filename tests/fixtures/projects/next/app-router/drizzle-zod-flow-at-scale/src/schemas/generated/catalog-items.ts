// @generated by scripts/generate-scale-fixtures.mts — do not edit
import { createInsertSchema, createSelectSchema } from "drizzle-zod";
import { z } from "zod";

import { catalogItems } from "../../db/schema.generated";

export const CreateCatalogItemSchema = createInsertSchema(catalogItems, {
  name: (schema) => schema.min(1).max(120),
  description: (schema) => schema.max(500).optional(),
}).omit({ id: true, createdAt: true, updatedAt: true });

export const UpdateCatalogItemSchema = CreateCatalogItemSchema.partial();

export const CatalogItemSchema = createSelectSchema(catalogItems);

export const CatalogItemListResponse = z.object({
  items: z.array(CatalogItemSchema),
  total: z.number().int().nonnegative(),
});

export const CatalogItemIdParamsSchema = z.object({
  id: z.string().regex(/^\d+$/),
});

export const CatalogItemListQuerySchema = z.object({
  page: z.string().regex(/^\d+$/).optional(),
  limit: z.string().regex(/^\d+$/).optional(),
});
