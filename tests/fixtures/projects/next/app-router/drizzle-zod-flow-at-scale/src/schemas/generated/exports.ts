// @generated by scripts/generate-scale-fixtures.mts — do not edit
import { createInsertSchema, createSelectSchema } from "drizzle-zod";
import { z } from "zod";

import { exports } from "../../db/schema.generated";

export const CreateExportSchema = createInsertSchema(exports, {
  name: (schema) => schema.min(1).max(120),
  description: (schema) => schema.max(500).optional(),
}).omit({ id: true, createdAt: true, updatedAt: true });

export const UpdateExportSchema = CreateExportSchema.partial();

export const ExportSchema = createSelectSchema(exports);

export const ExportListResponse = z.object({
  items: z.array(ExportSchema as unknown as z.ZodTypeAny),
  total: z.number().int().nonnegative(),
});

export const ExportIdParamsSchema = z.object({
  id: z.string().regex(/^\d+$/),
});

export const ExportListQuerySchema = z.object({
  page: z.string().regex(/^\d+$/).optional(),
  limit: z.string().regex(/^\d+$/).optional(),
});
