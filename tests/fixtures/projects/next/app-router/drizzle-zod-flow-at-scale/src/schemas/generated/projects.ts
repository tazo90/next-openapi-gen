// @generated by scripts/generate-scale-fixtures.mts — do not edit
import { createInsertSchema, createSelectSchema } from "drizzle-zod";
import { z } from "zod";

import { projects } from "../../db/schema.generated";

export const CreateProjectSchema = createInsertSchema(projects, {
  name: (schema) => schema.min(1).max(120),
  description: (schema) => schema.max(500).optional(),
}).omit({ id: true, createdAt: true, updatedAt: true });

export const UpdateProjectSchema = CreateProjectSchema.partial();

export const ProjectSchema = createSelectSchema(projects);

export const ProjectListResponse = z.object({
  items: z.array(ProjectSchema as unknown as z.ZodTypeAny),
  total: z.number().int().nonnegative(),
});

export const ProjectCollectionPathParamsSchema = z.object({
  organizationId: z.string().regex(/^\d+$/),
});

export const ProjectIdParamsSchema = z.object({
  organizationId: z.string().regex(/^\d+$/),
  projectId: z.string().regex(/^\d+$/),
});

export const ProjectListQuerySchema = z.object({
  page: z.string().regex(/^\d+$/).optional(),
  limit: z.string().regex(/^\d+$/).optional(),
});
