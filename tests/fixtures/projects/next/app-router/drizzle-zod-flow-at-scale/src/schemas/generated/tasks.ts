// @generated by scripts/generate-scale-fixtures.mts — do not edit
import { createInsertSchema, createSelectSchema } from "drizzle-zod";
import { z } from "zod";

import { tasks } from "../../db/schema.generated";

export const CreateTaskSchema = createInsertSchema(tasks, {
  name: (schema) => schema.min(1).max(120),
  description: (schema) => schema.max(500).optional(),
}).omit({ id: true, createdAt: true, updatedAt: true });

export const UpdateTaskSchema = CreateTaskSchema.partial();

export const TaskSchema = createSelectSchema(tasks);

export const TaskListResponse = z.object({
  items: z.array(TaskSchema),
  total: z.number().int().nonnegative(),
});

export const TaskCollectionPathParamsSchema = z.object({
  projectId: z.string().regex(/^\d+$/),
});

export const TaskIdParamsSchema = z.object({
  projectId: z.string().regex(/^\d+$/),
  id: z.string().regex(/^\d+$/),
});

export const TaskListQuerySchema = z.object({
  page: z.string().regex(/^\d+$/).optional(),
  limit: z.string().regex(/^\d+$/).optional(),
});
