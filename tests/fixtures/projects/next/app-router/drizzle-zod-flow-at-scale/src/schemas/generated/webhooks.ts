// @generated by scripts/generate-scale-fixtures.mts — do not edit
import { createInsertSchema, createSelectSchema } from "drizzle-zod";
import { z } from "zod";

import { webhooks } from "../../db/schema.generated";

export const CreateWebhookSchema = createInsertSchema(webhooks, {
  name: (schema) => schema.min(1).max(120),
  description: (schema) => schema.max(500).optional(),
}).omit({ id: true, createdAt: true, updatedAt: true });

export const UpdateWebhookSchema = CreateWebhookSchema.partial();

export const WebhookSchema = createSelectSchema(webhooks);

export const WebhookListResponse = z.object({
  items: z.array(WebhookSchema),
  total: z.number().int().nonnegative(),
});

export const WebhookIdParamsSchema = z.object({
  id: z.string().regex(/^\d+$/),
});

export const WebhookListQuerySchema = z.object({
  page: z.string().regex(/^\d+$/).optional(),
  limit: z.string().regex(/^\d+$/).optional(),
});
