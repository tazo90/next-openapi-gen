# Implementation Plans

Generated by the improve skill on 2026-08-18. Execute in the order below unless
dependencies say otherwise. Each executor: read the plan fully before starting,
honor its STOP conditions, and update your row when done.

This index is the `plan` variant: one requested change, no audit table.

## Execution order & status

| Plan | Title                                                      | Priority | Effort | Depends on | Status |
| ---- | ---------------------------------------------------------- | -------- | ------ | ---------- | ------ |
| 001  | Emit usable securitySchemes and combined auth requirements | P1       | M      | —          | DONE   |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale — finding fixed independently or approach abandoned)

## Dependency notes

- 001 is standalone. Do not regenerate scale fixtures or spread the Security pack to other apps as part of this plan.

## Findings considered and rejected

- Generating full OAuth/OIDC/mTLS scheme objects from JSDoc: rejected for this slice. That is a new config surface the repo has been trying not to grow. Custom flows stay in `components` / `schemaFiles`.
- Adding cookie as a fourth built-in preset: rejected. Cookie names are app-specific (`session` vs `sb-access-token`). The Zod sample defines `SessionCookie` in config.
- Rewriting `authPresets` from `Record<string, string>` to scheme objects: rejected. Presets keep mapping names; only the three built-in keywords get default objects when referenced and missing.
- Spreading the Security pack across TypeScript and framework-parity apps: deferred. The request asked for one sample.
