{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "outputs": ["dist/**", "templates/**", "tsconfig.tsbuildinfo"]
    },
    "test:coverage": {
      "outputs": ["coverage/**"]
    }
  }
}
