{
  "name": "@workspace/oxfmt-config",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "format": "oxfmt --config ./oxfmt.json --ignore-path ../../.gitignore .",
    "format:check": "oxfmt --config ./oxfmt.json --ignore-path ../../.gitignore --check ."
  }
}
